name: Terraform IAM Pipeline

on:
  push:
    branches:
      - main
    paths-ignore:
      - "README.md"
      - "docs/**"
  pull_request:
    branches:
      - main
  # Nightly drift detection — disabled until a prod environment exists.
  # Re-enable once prod secrets (AZURE_*_PROD, KEY_VAULT_NAME_PROD) are configured.
  # schedule:
  #   - cron: "0 2 * * *"
  workflow_dispatch:
    inputs:
      environment:
        description: "Target environment (dev or prod)"
        required: true
        default: "dev"
        type: choice
        options:
          - dev
          - prod
      action:
        description: "Terraform action to run"
        required: true
        default: plan
        type: choice
        options:
          - plan
          - apply

env:
  TF_VERSION: "1.7.0"
  WORKING_DIR: "."

# Prevents two runs from racing against the same state file —
# e.g. a push to main and a manual dispatch targeting the same environment
concurrency:
  group: terraform-${{ github.event.inputs.environment || 'dev' }}
  cancel-in-progress: false

jobs:
  validate:
    name: "Validate & Lint"
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: ${{ env.TF_VERSION }}

      - name: Terraform Format Check
        run: terraform fmt -check -recursive
        working-directory: ${{ env.WORKING_DIR }}

      - name: Terraform Init (validate only)
        run: terraform init -backend=false
        working-directory: ${{ env.WORKING_DIR }}

      - name: Terraform Validate
        run: terraform validate -no-color
        working-directory: ${{ env.WORKING_DIR }}

      - uses: terraform-linters/setup-tflint@v4
        with:
          tflint_version: latest

      - run: tflint --init

      - run: tflint --format compact --recursive

      # Checkov blocks the pipeline on CRITICAL/HIGH findings instead of
      # always passing. soft_fail only applies to LOW/MEDIUM so the scan still
      # surfaces minor issues without blocking on them.
      - name: Checkov (block on HIGH/CRITICAL)
        uses: bridgecrewio/checkov-action@v12
        with:
          directory: ${{ env.WORKING_DIR }}
          framework: terraform
          soft_fail_on: LOW,MEDIUM
          hard_fail_on: HIGH,CRITICAL
          output_format: cli

  # ── Pre-Apply Setup ──────────────────────────────────────────────────────────
  # Runs before apply only. Ensures the IAM resource group exists and grants
  # the Terraform SP the roles it needs to deploy the monitoring module:
  #   - Role Based Access Control Administrator (constrained to Sentinel Responder)
  #   - Microsoft Sentinel Contributor
  #
  # These roles live on the IAM resource group, which Terraform itself creates.
  # That creates a bootstrap dependency: the SP needs the roles before it can
  # deploy, but the resource group doesn't exist until Terraform runs.
  # This job resolves that by creating the resource group and assigning the
  # roles as a pipeline step, before the plan and apply jobs run.
  #
  # Safe to re-run — duplicate role assignments are detected and skipped.
  setup:
    name: "Pre-Apply Setup"
    runs-on: ubuntu-latest
    needs: validate
    if: github.event_name == 'workflow_dispatch' && github.event.inputs.action == 'apply'

    permissions:
      id-token: write
      contents: read

    steps:
      - name: Azure Login (DEV)
        if: ${{ (github.event.inputs.environment || 'dev') != 'prod' }}
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID_DEV }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID_DEV }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID_DEV }}

      - name: Azure Login (PROD)
        if: ${{ github.event.inputs.environment == 'prod' }}
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID_PROD }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID_PROD }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID_PROD }}

      - name: Set environment variables (DEV)
        if: ${{ (github.event.inputs.environment || 'dev') != 'prod' }}
        run: |
          echo "SUBSCRIPTION_ID=${{ secrets.AZURE_SUBSCRIPTION_ID_DEV }}" >> $GITHUB_ENV
          echo "CLIENT_ID=${{ secrets.AZURE_CLIENT_ID_DEV }}" >> $GITHUB_ENV

      - name: Set environment variables (PROD)
        if: ${{ github.event.inputs.environment == 'prod' }}
        run: |
          echo "SUBSCRIPTION_ID=${{ secrets.AZURE_SUBSCRIPTION_ID_PROD }}" >> $GITHUB_ENV
          echo "CLIENT_ID=${{ secrets.AZURE_CLIENT_ID_PROD }}" >> $GITHUB_ENV

      - name: Ensure IAM resource group exists
        run: |
          ENV="${{ github.event.inputs.environment || 'dev' }}"
          IAM_RG="rg-${{ vars.COMPANY_NAME }}-${ENV}-iam"
          az group create \
            --name "$IAM_RG" \
            --location "${{ vars.AZURE_LOCATION }}" \
            --output none
          echo "IAM_RG=$IAM_RG" >> $GITHUB_ENV

      - name: Assign Role Based Access Control Administrator (constrained)
        run: |
          SP_OBJECT_ID=$(az ad sp show --id "$CLIENT_ID" --query id -o tsv)
          SENTINEL_RESPONDER_ID="3e150937-b8fe-4cfb-8069-0eaf05ecd056"
          SCOPE="/subscriptions/$SUBSCRIPTION_ID/resourceGroups/$IAM_RG"

          EXISTING=$(az role assignment list \
            --assignee "$SP_OBJECT_ID" \
            --scope "$SCOPE" \
            --query "[?roleDefinitionName=='Role Based Access Control Administrator'].id" \
            -o tsv 2>/dev/null || echo "")

          if [[ -n "$EXISTING" ]]; then
            echo "RBAC Administrator already assigned — skipping"
          else
            az role assignment create \
              --assignee-object-id "$SP_OBJECT_ID" \
              --assignee-principal-type ServicePrincipal \
              --role "Role Based Access Control Administrator" \
              --scope "$SCOPE" \
              --condition "((!(ActionMatches{'Microsoft.Authorization/roleAssignments/write'})) OR (@Request[Microsoft.Authorization/roleAssignments:RoleDefinitionId] ForAnyOfAnyValues:GuidEquals {$SENTINEL_RESPONDER_ID} AND @Request[Microsoft.Authorization/roleAssignments:PrincipalType] ForAnyOfAnyValues:StringEqualsIgnoreCase {'ServicePrincipal', 'ManagedIdentity'})) AND ((!(ActionMatches{'Microsoft.Authorization/roleAssignments/delete'})) OR (@Resource[Microsoft.Authorization/roleAssignments:RoleDefinitionId] ForAnyOfAnyValues:GuidEquals {$SENTINEL_RESPONDER_ID} AND @Resource[Microsoft.Authorization/roleAssignments:PrincipalType] ForAnyOfAnyValues:StringEqualsIgnoreCase {'ServicePrincipal', 'ManagedIdentity'}))" \
              --condition-version "2.0" \
              --output none
            echo "RBAC Administrator assigned"
          fi

      - name: Assign Microsoft Sentinel Contributor
        run: |
          SP_OBJECT_ID=$(az ad sp show --id "$CLIENT_ID" --query id -o tsv)
          SCOPE="/subscriptions/$SUBSCRIPTION_ID/resourceGroups/$IAM_RG"

          EXISTING=$(az role assignment list \
            --assignee "$SP_OBJECT_ID" \
            --scope "$SCOPE" \
            --query "[?roleDefinitionName=='Microsoft Sentinel Contributor'].id" \
            -o tsv 2>/dev/null || echo "")

          if [[ -n "$EXISTING" ]]; then
            echo "Microsoft Sentinel Contributor already assigned — skipping"
          else
            az role assignment create \
              --assignee-object-id "$SP_OBJECT_ID" \
              --assignee-principal-type ServicePrincipal \
              --role "Microsoft Sentinel Contributor" \
              --scope "$SCOPE" \
              --output none
            echo "Microsoft Sentinel Contributor assigned"
          fi

      - name: Wait for role assignments to propagate
        run: sleep 30

  plan:
    name: "Terraform Plan"
    runs-on: ubuntu-latest
    needs: [validate, setup]
    if: |
      always() &&
      needs.validate.result == 'success' &&
      (needs.setup.result == 'success' || needs.setup.result == 'skipped')
    # Scheduled drift detection runs do NOT use an environment — they only
    # read state and run a plan, so there is nothing to gate. Approval gates
    # are only needed for manual dispatches that could lead to an apply.
    environment: ${{ github.event_name != 'schedule' && (github.event.inputs.environment || 'dev') || '' }}
    outputs:
      exitcode: ${{ steps.plan.outputs.exitcode }}

    permissions:
      id-token: write
      contents: read
      pull-requests: write

    steps:
      - uses: actions/checkout@v4

      - uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: ${{ env.TF_VERSION }}

      - name: Azure Login (DEV)
        if: ${{ github.event_name != 'schedule' && (github.event.inputs.environment || 'dev') != 'prod' }}
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID_DEV }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID_DEV }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID_DEV }}

      - name: Azure Login (PROD — manual dispatch)
        if: ${{ github.event_name != 'schedule' && github.event.inputs.environment == 'prod' }}
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID_PROD }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID_PROD }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID_PROD }}

      - name: Azure Login (PROD — drift detection)
        if: ${{ github.event_name == 'schedule' }}
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID_PROD }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID_PROD }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID_PROD }}

      - name: Set Terraform ARM env vars (DEV)
        if: ${{ github.event_name != 'schedule' && (github.event.inputs.environment || 'dev') != 'prod' }}
        run: |
          echo "ARM_CLIENT_ID=${{ secrets.AZURE_CLIENT_ID_DEV }}" >> $GITHUB_ENV
          echo "ARM_SUBSCRIPTION_ID=${{ secrets.AZURE_SUBSCRIPTION_ID_DEV }}" >> $GITHUB_ENV
          echo "ARM_TENANT_ID=${{ secrets.AZURE_TENANT_ID_DEV }}" >> $GITHUB_ENV
          echo "ARM_ACCESS_KEY=${{ secrets.TF_STATE_ACCESS_KEY_DEV }}" >> $GITHUB_ENV
          echo "ARM_USE_OIDC=true" >> $GITHUB_ENV
          echo "TF_VAR_subscription_id=${{ secrets.AZURE_SUBSCRIPTION_ID_DEV }}" >> $GITHUB_ENV
          echo "TF_VAR_tenant_id=${{ secrets.AZURE_TENANT_ID_DEV }}" >> $GITHUB_ENV
          echo "TF_VAR_tenant_domain=${{ secrets.AZURE_TENANT_DOMAIN_DEV }}" >> $GITHUB_ENV
          echo "TF_VAR_alert_email=${{ secrets.ALERT_EMAIL }}" >> $GITHUB_ENV
          echo "TF_VAR_key_vault_name=${{ secrets.KEY_VAULT_NAME_DEV }}" >> $GITHUB_ENV

      - name: Set Terraform ARM env vars (PROD)
        if: ${{ github.event_name == 'schedule' || github.event.inputs.environment == 'prod' }}
        run: |
          echo "ARM_CLIENT_ID=${{ secrets.AZURE_CLIENT_ID_PROD }}" >> $GITHUB_ENV
          echo "ARM_SUBSCRIPTION_ID=${{ secrets.AZURE_SUBSCRIPTION_ID_PROD }}" >> $GITHUB_ENV
          echo "ARM_TENANT_ID=${{ secrets.AZURE_TENANT_ID_PROD }}" >> $GITHUB_ENV
          echo "ARM_ACCESS_KEY=${{ secrets.TF_STATE_ACCESS_KEY_PROD }}" >> $GITHUB_ENV
          echo "ARM_USE_OIDC=true" >> $GITHUB_ENV
          echo "TF_VAR_subscription_id=${{ secrets.AZURE_SUBSCRIPTION_ID_PROD }}" >> $GITHUB_ENV
          echo "TF_VAR_tenant_id=${{ secrets.AZURE_TENANT_ID_PROD }}" >> $GITHUB_ENV
          echo "TF_VAR_tenant_domain=${{ secrets.AZURE_TENANT_DOMAIN_PROD }}" >> $GITHUB_ENV
          echo "TF_VAR_alert_email=${{ secrets.ALERT_EMAIL }}" >> $GITHUB_ENV
          echo "TF_VAR_key_vault_name=${{ secrets.KEY_VAULT_NAME_PROD }}" >> $GITHUB_ENV

      - name: Terraform Init
        run: terraform init
        working-directory: ${{ env.WORKING_DIR }}

      # -detailed-exitcode: 0 = no changes, 1 = error, 2 = changes present.
      # This is what makes drift detection meaningful — the workflow can react
      # differently depending on whether drift was actually found.
      - name: Terraform Plan
        id: plan
        run: |
          set +e
          terraform plan \
            -var="environment=${{ github.event_name == 'schedule' && 'prod' || (github.event.inputs.environment || 'dev') }}" \
            -out=tfplan \
            -detailed-exitcode \
            -no-color
          echo "exitcode=$?" >> "$GITHUB_OUTPUT"
          exit 0
        working-directory: ${{ env.WORKING_DIR }}

      - name: Fail on plan error
        if: steps.plan.outputs.exitcode == 1
        run: |
          echo "::error::Terraform plan failed (exit code 1)"
          exit 1

      - name: Flag drift (scheduled run only)
        if: github.event_name == 'schedule' && steps.plan.outputs.exitcode == 2
        run: |
          echo "::warning::Drift detected in prod — infrastructure differs from Terraform state. Review the plan output and reconcile manually or via a follow-up apply."

      - name: Upload Plan Artifact
        if: github.event_name != 'schedule'
        uses: actions/upload-artifact@v4
        with:
          name: tfplan-${{ github.event.inputs.environment || 'dev' }}-${{ github.run_id }}
          path: tfplan
          retention-days: 30

      - name: Post Plan to PR
        if: github.event_name == 'pull_request'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          terraform show -no-color tfplan > plan.txt
          {
            echo "#### Terraform Plan — ${{ github.event.inputs.environment || 'dev' }} 🔍"
            echo '```'
            # Show full plan, but cap at GitHub's comment size limits.
            # Full plan.txt is always available as a workflow artifact above.
            tail -c 60000 plan.txt
            echo '```'
            echo "_Full plan available in the [workflow run artifacts](${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }})._"
          } > comment.md
          gh pr comment ${{ github.event.pull_request.number }} --body-file comment.md
        working-directory: ${{ env.WORKING_DIR }}

  apply:
    name: "Terraform Apply"
    runs-on: ubuntu-latest
    needs: [plan, setup]
    if: github.event_name == 'workflow_dispatch' && github.event.inputs.action == 'apply'
    environment: ${{ github.event.inputs.environment || 'dev' }}

    permissions:
      id-token: write
      contents: read

    steps:
      - uses: actions/checkout@v4

      - uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: ${{ env.TF_VERSION }}

      - name: Azure Login (DEV)
        if: ${{ (github.event.inputs.environment || 'dev') != 'prod' }}
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID_DEV }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID_DEV }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID_DEV }}

      - name: Azure Login (PROD)
        if: ${{ github.event.inputs.environment == 'prod' }}
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID_PROD }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID_PROD }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID_PROD }}

      - name: Set Terraform ARM env vars (DEV)
        if: ${{ (github.event.inputs.environment || 'dev') != 'prod' }}
        run: |
          echo "ARM_CLIENT_ID=${{ secrets.AZURE_CLIENT_ID_DEV }}" >> $GITHUB_ENV
          echo "ARM_SUBSCRIPTION_ID=${{ secrets.AZURE_SUBSCRIPTION_ID_DEV }}" >> $GITHUB_ENV
          echo "ARM_TENANT_ID=${{ secrets.AZURE_TENANT_ID_DEV }}" >> $GITHUB_ENV
          echo "ARM_ACCESS_KEY=${{ secrets.TF_STATE_ACCESS_KEY_DEV }}" >> $GITHUB_ENV
          echo "ARM_USE_OIDC=true" >> $GITHUB_ENV
          echo "TF_VAR_subscription_id=${{ secrets.AZURE_SUBSCRIPTION_ID_DEV }}" >> $GITHUB_ENV
          echo "TF_VAR_tenant_id=${{ secrets.AZURE_TENANT_ID_DEV }}" >> $GITHUB_ENV
          echo "TF_VAR_tenant_domain=${{ secrets.AZURE_TENANT_DOMAIN_DEV }}" >> $GITHUB_ENV
          echo "TF_VAR_alert_email=${{ secrets.ALERT_EMAIL }}" >> $GITHUB_ENV
          echo "TF_VAR_key_vault_name=${{ secrets.KEY_VAULT_NAME_DEV }}" >> $GITHUB_ENV

      - name: Set Terraform ARM env vars (PROD)
        if: ${{ github.event.inputs.environment == 'prod' }}
        run: |
          echo "ARM_CLIENT_ID=${{ secrets.AZURE_CLIENT_ID_PROD }}" >> $GITHUB_ENV
          echo "ARM_SUBSCRIPTION_ID=${{ secrets.AZURE_SUBSCRIPTION_ID_PROD }}" >> $GITHUB_ENV
          echo "ARM_TENANT_ID=${{ secrets.AZURE_TENANT_ID_PROD }}" >> $GITHUB_ENV
          echo "ARM_ACCESS_KEY=${{ secrets.TF_STATE_ACCESS_KEY_PROD }}" >> $GITHUB_ENV
          echo "ARM_USE_OIDC=true" >> $GITHUB_ENV
          echo "TF_VAR_subscription_id=${{ secrets.AZURE_SUBSCRIPTION_ID_PROD }}" >> $GITHUB_ENV
          echo "TF_VAR_tenant_id=${{ secrets.AZURE_TENANT_ID_PROD }}" >> $GITHUB_ENV
          echo "TF_VAR_tenant_domain=${{ secrets.AZURE_TENANT_DOMAIN_PROD }}" >> $GITHUB_ENV
          echo "TF_VAR_alert_email=${{ secrets.ALERT_EMAIL }}" >> $GITHUB_ENV
          echo "TF_VAR_key_vault_name=${{ secrets.KEY_VAULT_NAME_PROD }}" >> $GITHUB_ENV

      - name: Terraform Init
        run: terraform init
        working-directory: ${{ env.WORKING_DIR }}

      - name: Download Plan Artifact
        uses: actions/download-artifact@v4
        with:
          name: tfplan-${{ github.event.inputs.environment || 'dev' }}-${{ github.run_id }}
          path: ${{ env.WORKING_DIR }}

      # Re-checks the plan against current state right before applying.
      # If something changed between plan and apply (e.g. manual portal edit,
      # or someone else's apply landed first), this catches it instead of
      # blindly applying a now-stale plan.
      - name: Verify plan is still current
        run: |
          set +e
          terraform plan -detailed-exitcode -out=tfplan-recheck -no-color \
            -var="environment=${{ github.event.inputs.environment || 'dev' }}"
          code=$?
          if [ "$code" -eq 2 ]; then
            echo "::error::State has drifted since the plan was generated. Re-run plan before applying."
            exit 1
          elif [ "$code" -eq 1 ]; then
            echo "::error::Terraform plan re-check failed."
            exit 1
          fi
        working-directory: ${{ env.WORKING_DIR }}

      - name: Terraform Apply
        run: terraform apply -auto-approve tfplan
        working-directory: ${{ env.WORKING_DIR }}

      - name: Terraform Output
        run: terraform output -json > tf-outputs-${{ github.event.inputs.environment || 'dev' }}.json
        working-directory: ${{ env.WORKING_DIR }}

      - name: Upload Outputs
        uses: actions/upload-artifact@v4
        with:
          name: tf-outputs-${{ github.event.inputs.environment || 'dev' }}-${{ github.run_id }}
          path: tf-outputs-${{ github.event.inputs.environment || 'dev' }}.json
          retention-days: 90